package at.htl.firedepartment;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeResourceIT extends VehicleResourceTest {

    // Execute the same tests but in native mode.
}